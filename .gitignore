# Java / Maven
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# Node / Vite
node_modules/
dist/
.vite/

# Secrets — API keys are read from here and must never be committed
.env
.env.*
!.env.example

# Embedded H2 database files (DM-6)
data/
*.mv.db
*.trace.db

# IDE
.idea/
*.iml
.vscode/

# Per-agent git worktrees (Phase 2-5 parallel work) — each is its own checkout on its
# own branch; committing one into main duplicates the whole tree (see git history).
.claude/worktrees/

# OS
.DS_Store
Thumbs.db
