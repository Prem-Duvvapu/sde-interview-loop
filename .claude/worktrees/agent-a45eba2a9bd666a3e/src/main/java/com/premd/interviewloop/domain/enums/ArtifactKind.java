package com.premd.interviewloop.domain.enums;

/**
 * Kind of artifact snapshot stored alongside transcript turns.
 */
public enum ArtifactKind {
    CODE,
    CLASS_MODEL,
    DIAGRAM,
    SCRATCH
}
